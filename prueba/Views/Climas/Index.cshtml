@model IEnumerable<prueba.Models.Clima>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreClima)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreClima)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClimaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ClimaId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ClimaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
