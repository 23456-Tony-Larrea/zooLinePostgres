@model IEnumerable<prueba.Models.Continente>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Crear nuevo </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreContinente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreContinente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContinenteId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ContinenteId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ContinenteId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
